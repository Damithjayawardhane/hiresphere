# Copy to 01-secret.yaml (gitignored) and adjust values before apply.
# kubectl create secret generic hiresphere-secrets -n hiresphere --from-literal=SECRET_KEY=your-secret --dry-run=client -o yaml > 01-secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: hiresphere-secrets
  namespace: hiresphere
type: Opaque
stringData:
  SECRET_KEY: "change-me-in-production"
