apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: hiresphere
  labels:
    app: auth-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
    spec:
      containers:
        - name: auth-service
          image: hiresphere/auth-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5001
          env:
            - name: SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: hiresphere-secrets
                  key: SECRET_KEY
            - name: DATABASE_URL
              value: "sqlite:////data/auth.db"
            - name: COGNITO_USER_POOL_ID
              value: ""
            - name: COGNITO_APP_CLIENT_ID
              value: ""
            - name: AWS_REGION
              value: "us-east-1"
          volumeMounts:
            - name: data
              mountPath: /data
          livenessProbe:
            httpGet:
              path: /health
              port: 5001
            initialDelaySeconds: 15
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /health
              port: 5001
            initialDelaySeconds: 5
            periodSeconds: 10
      volumes:
        - name: data
          emptyDir: {}
